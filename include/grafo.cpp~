#include <vector>

//Usamos template para generalizar nuestra implementacion, definiendo el tipo de vertice y por defecto el peso de las aristas como un entero
template <typename TipoVertice, typename TipoPeso = int>
  class Grafo{
 public:
 virtual ~Grafo() = default;


 // Comprobacion booleana para saber si el grafo es dirigido o no

 virtual bool esDirigido() const = 0;

 
 
  
  
